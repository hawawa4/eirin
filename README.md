NOTE FOR UBUNTU/DEBIAN: This uses Wails, and Wails by default uses a libwebkit that is not available.

`sudo apt install libwebkit2gtk-4.1-dev`

All wails command then need the parameter

`wails dev -tags webkit2_41`


